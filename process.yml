apps:
  - script : index.js
    name   : teli-delay-1
    instances: 1
    watch  : false
    exec_mode  : fork
    env    :
      NODE_ENV: development
    env_production:
      NODE_ENV: production
